.row
  .col-lg-8.col-lg-offset-2
    %table.table.table-condensed
      %tr
        %th= 'Nazwa instytutu'
        %th= 'Details'
        %th
      -@institutes.each do |institute|
        %tr
          %td= institute.name
          %td=link_to "Show", institute_path(institute[:id]), class: 'btn btn-primary'
          %td=link_to "Edit", edit_institute_path(institute[:id]), class: 'btn btn-primary'
.row
  .col-lg-8.col-lg-offset-2
    =link_to "New", new_institute_path, class: 'btn btn-primary'